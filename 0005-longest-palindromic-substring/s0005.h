#include <algorithm>
#include <vector>
#include <string>
#include <assert.h>
#include <stdlib.h>
using namespace std;

class Solution0005 {
public:
	string longestPalindrome(string s) {
		/*
		const char* p = s.c_str();
		const int len = s.length();

		int max = 0;
		int beg = 0, end = 0;

		int k = 0;
		while (k)
		{

		}
		*/
		return s;
	}
};